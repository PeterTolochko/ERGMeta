% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methodology_roadmap_functions.R
\name{methodology_roadmap}
\alias{methodology_roadmap}
\title{Run a Concrete Methodological Roadmap}
\usage{
methodology_roadmap(
  fit_list,
  grouping_variable = NULL,
  strict_terms = TRUE,
  include_leave_one_out = TRUE,
  exclude_sets = NULL,
  sensitivity_method = c("random", "fixed"),
  moderators = NULL,
  moderator_df = NULL,
  reference_moderators = NULL,
  fit_covariance_model = TRUE,
  covariance_method = c("random", "fixed")
)
}
\arguments{
\item{fit_list}{A non-empty list that may include fitted \code{ergm} models and failed fits (\code{NULL}).}

\item{grouping_variable}{(Optional) A network-level attribute used for grouping.}

\item{strict_terms}{Logical. Passed to \code{extract_within_covariance()}.}

\item{include_leave_one_out}{Logical. Passed to \code{run_exclusion_sensitivity()}.}

\item{exclude_sets}{Optional named list of integer vectors passed to \code{run_exclusion_sensitivity()}.}

\item{sensitivity_method}{Estimation mode for sensitivity: \code{"random"} (default) or \code{"fixed"}.}

\item{moderators}{Optional character vector of network-level moderator names.}

\item{moderator_df}{Optional data frame with one row per network and moderator columns.}

\item{reference_moderators}{Optional named list or one-row data frame for reference predictions.}

\item{fit_covariance_model}{Logical. If \code{TRUE}, fit \code{meta_fit_covariance()}.}

\item{covariance_method}{Estimation mode for covariance-aware fitting: \code{"random"} (default) or \code{"fixed"}.}
}
\value{
A list with prepared meta-analysis inputs, exclusion-sensitivity summaries,
and an optional covariance-aware multivariate fit.
}
\description{
Run a full methodological workflow from fitted networks: prepare estimates and covariance,
track exclusions, and quantify sensitivity to network removal scenarios.
}
